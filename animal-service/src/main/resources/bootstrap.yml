spring:
  application:
    name: animal-service
  cloud:
    config:
      uri: http://${CONFIG_SERVER_HOST}
      username: ${CONFIG_USERNAME}
      password: ${CONFIG_PASSWORD}
      fail-fast: true
      retry:
        max-attempts: 6
        initial-interval: 1000
