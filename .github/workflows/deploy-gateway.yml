name: Deploy ApiGateway Service

on:
  push:
    branches: [ "dev" ]
    paths:
      - 'api-gateway/**' # 이 폴더가 변했을 때만 실행!
      - '.github/workflows/deploy-gateway.yml'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      # 1. 빌드 (해당 폴더로 이동해서 실행)
      - name: Build Gateway Service
        run: |
          cd api-gateway  # 해당 서비스 폴더로 이동
          chmod +x gradlew
          ./gradlew clean build -x test

      - name: Docker Login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 2. 이미지 빌드 & 푸시
      - name: Build & Push Docker Image
        run: |
          cd api-gateway
          docker build -t ${{ secrets.DOCKER_USERNAME }}/api-gateway:latest .
          docker push ${{ secrets.DOCKER_USERNAME }}/api-gateway:latest

      # 3. 배포 (Node-1로 전송)
      - name: Deploy to Node-3
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.EC2_HOST_1 }} # Gateway는 Node-1
          username: ubuntu
          key: ${{ secrets.EC2_KEY }}
          script: |
            cd ~/deploy
            # 환경변수 주입 (필요한 경우)
            echo "DOCKER_USERNAME=${{ secrets.DOCKER_USERNAME }}" > .env
            echo "JWT_SECRET=${{ secrets.JWT_SECRET }}" >> .env

            # Service URLs (Node-2의 user-service로 라우팅)
            echo "USER_SERVICE_URL=http://${{ secrets.NODE2_PRIVATE_IP }}:8081" >> .env
            echo "EMAIL_SERVICE_URL=http://${{ secrets.NODE2_PRIVATE_IP }}:8088" >> .env

            # 프라이빗 IP 주입
            echo "NODE1_PRIVATE_IP=${{ secrets.NODE1_PRIVATE_IP }}" >> .env
            echo "NODE2_PRIVATE_IP=${{ secrets.NODE2_PRIVATE_IP }}" >> .env
            echo "NODE3_PRIVATE_IP=${{ secrets.NODE3_PRIVATE_IP }}" >> .env
            
            # 해당 서비스만 콕 집어서 재시작
            docker compose pull api-gateway
            docker compose up -d --no-deps api-gateway
            docker image prune -f