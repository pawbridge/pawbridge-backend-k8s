# Explicit migration profile; default runtime remains unchanged until approved cutover.
spring:
  datasource:
    url: ${USER_POSTGRESQL_JDBC_URL}
    username: ${USER_POSTGRESQL_USERNAME}
    password: ${USER_POSTGRESQL_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      schema: pawbridge_user
      connection-init-sql: SET TIME ZONE 'UTC'
      maximum-pool-size: ${USER_POSTGRESQL_POOL_MAX:5}
      minimum-idle: ${USER_POSTGRESQL_POOL_MIN:0}
      connection-timeout: 5000
      validation-timeout: 1000
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: pawbridge_user
        jdbc.time_zone: UTC
  sql:
    init:
      mode: never
