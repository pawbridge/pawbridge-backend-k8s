# Explicit migration profile; default runtime remains unchanged until approved cutover.
spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.elasticsearch.ElasticsearchClientAutoConfiguration
      - org.springframework.boot.autoconfigure.elasticsearch.ElasticsearchRestClientAutoConfiguration
      - org.springframework.boot.autoconfigure.elasticsearch.ReactiveElasticsearchClientAutoConfiguration
      - org.springframework.boot.autoconfigure.data.elasticsearch.ElasticsearchDataAutoConfiguration
      - org.springframework.boot.autoconfigure.data.elasticsearch.ElasticsearchRepositoriesAutoConfiguration
      - org.springframework.boot.autoconfigure.data.elasticsearch.ReactiveElasticsearchRepositoriesAutoConfiguration
  datasource:
    url: ${STORE_POSTGRESQL_JDBC_URL}
    username: ${STORE_POSTGRESQL_USERNAME}
    password: ${STORE_POSTGRESQL_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      schema: pawbridge_store
      connection-init-sql: SET TIME ZONE 'UTC'
      maximum-pool-size: ${STORE_POSTGRESQL_POOL_MAX:5}
      minimum-idle: ${STORE_POSTGRESQL_POOL_MIN:0}
      connection-timeout: 5000
      validation-timeout: 1000
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: pawbridge_store
        jdbc.time_zone: UTC
  sql:
    init:
      mode: never

pawbridge:
  search:
    backend: postgresql
    rebuild-enabled: ${SEARCH_REBUILD_ENABLED:false}
